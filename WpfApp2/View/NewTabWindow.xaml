<Window x:Class="WpfApp2.View.NewTabWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2.View"
        xmlns:vm="clr-namespace:WpfApp2.ViewModel"
        mc:Ignorable="d"
        Title="New tab" Height="100" Width="200">
    <Window.DataContext>
        <vm:NewTabViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Type:" Margin="5"/>
        <ComboBox Grid.Column="1" Grid.ColumnSpan="2" ItemsSource="{Binding Types, Mode=OneTime}" SelectedItem="{Binding SelectedType.Value}" Margin="5"/>
        <Button Grid.Row="2" Grid.Column="2" Command="{Binding AddCommand}" Margin="5" Width="75">OK</Button>
    </Grid>
</Window>
